# Бот работает на Linux, а правится с Windows. Без этой строки git
# отдаёт файлы с виндовыми переводами строк, и установочный скрипт на
# сервере падает с «set: pipefail: invalid option name» — так уже было.
* text=auto eol=lf
